import style from './index.module.scss';

const MovieModal = () => {
  return (
    <div className={style.mask}>
      <div className={style.wrapper}>modal</div>
    </div>
  );
};

export default MovieModal;
